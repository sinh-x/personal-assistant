name: Nix Build

on:
  push:
    branches: [main, develop]
    paths:
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'flake.nix'
      - 'flake.lock'
  pull_request:
    branches: [main, develop]
    paths:
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'flake.nix'
      - 'flake.lock'

jobs:
  nix-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main

      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Build personal-assistant
        run: |
          if ! nix build .#personal-assistant --print-build-logs; then
            echo "::error::nix build failed. If the error mentions ERR_PNPM_NO_OFFLINE_TARBALL or a hash mismatch,"
            echo "::error::run \`bash scripts/dev/version_bump.sh --refresh-hash\` locally, commit flake.nix, and re-push."
            exit 1
          fi
